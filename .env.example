# Copy to .env and fill in. The .env file is git-ignored.
OPENAI_API_KEY=sk-your-key-here

# Optional: override every model role at once (LangChain "provider:model" syntax).
# Leave unset to use the models defined in backend/config.yaml (default: openai:gpt-5-mini).
# MODEL=openai:gpt-5-mini
